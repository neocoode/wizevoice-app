import { ViewStyle } from 'react-native';
import styled from 'styled-components/native';

export const Container = styled.View`
  display: flex;
  width: 100%;
  height: 100%;
  justify-content: center;
  align-items: center;
`;
export const mapView: ViewStyle = {
  display: 'flex',
  width: '100%',
  height: '100%',
};

export const Map = styled.View`
  flex: 1;
  width: 100%;
`; 